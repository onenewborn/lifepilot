# memory-capture

这个 skill 用于把用户明确表达的长期饮食偏好整理成待确认记忆候选。它不直接写入长期偏好。

## 支持能力

- 识别“以后”“下次”“记住”“少推荐”“多推荐”等长期偏好表达。
- 生成 `memory_prompts` 给前端展示。
- 生成结构化 `memory_capture` 调用，让后端创建 pending candidate。

## 产品价值

LifePilot 的个性化必须可控。用户表达偏好后，小汪可以先整理，但要等用户确认后才长期记住。这让推荐越来越懂用户，同时保留用户控制权。

