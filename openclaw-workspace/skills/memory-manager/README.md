# memory-manager

这个 skill 用于执行用户明确授权的记忆管理操作，例如确认、拒绝、修改、删除、暂停或查询记忆。

## 支持能力

- 确认最近一条待确认记忆。
- 拒绝或删除不想保留的偏好。
- 创建用户明确要求长期保存的偏好。
- 修改或暂停已有偏好。
- 查询当前记忆账本。

## 产品价值

个性化只有在用户能控制时才值得信任。`memory-manager` 让小汪可以用自然语言理解“刚刚那条别记了”“这条确认下来”，但真正写入仍由后端执行和审计。

