# m_admin_1780588388769_aeb218

这个目录是后台管理流程生成的商户媒体目录，用于保存某次管理端上传或烟测产生的资产路径。

## 说明

目录名来自后台管理工具生成的商户或上传 slug。它不是业务规则，也不是推荐系统的权威数据。真正的商户信息仍以 `data/synthetic_food_futian/` 和后端 API 返回为准。

## 维护原则

- 如果这里没有正式资产，可以在后续清理后台测试素材时一并删除。
- 如果保留资产，应确保数据文件中的路径能对应到这里或 COS 上的正式文件。

