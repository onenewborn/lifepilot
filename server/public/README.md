# public

这个目录保存后端直接服务的静态页面或静态资产。当前主要是后台管理页面，用于维护商户、方向、offer、deal、口碑和记忆调试信息。

## 支持能力

- `admin/`：浏览器后台管理工具和 Memory Pipeline 调试面板。

## 产品价值

LifePilot 的推荐质量依赖结构化数据。后台页面让数据维护和调试更可视化，减少直接手改 JSON 的风险。

## 维护原则

- 面向用户的小程序页面不放在这里。
- 后台页面应使用 `/api/admin/*`，不要绕过后端校验直接写数据。

